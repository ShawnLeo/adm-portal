import {joint, formator, dateFormat} from './utils.js';
import {getStore, removeStore, setStore} from './storage.js';
let utils = {joint, formator, dateFormat, getStore, removeStore, setStore};
export default utils;
